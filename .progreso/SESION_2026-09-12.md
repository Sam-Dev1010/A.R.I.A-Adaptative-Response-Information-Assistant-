# SESIÓN 2026-09-12 — Escalado de datos y reentrenado por lotes

## Qué se hizo
1. **Corpus escalado (Wikipedia es)**: `data/neural/harvest_masivo.py` (reanudable,
   API action=query generator=random, extintro, throttle 1s, UA header, backoff en
   429/5xx). Estado: `corpus_masivo.json` = **3342 textos / 3.11 MB**.
   Progreso guardado en `harvest_state.json` y log en `harvest.log`.
2. **Reentrenado de `scaled_model_v3`** con `data/neural/retrain_v3.py`:
   - Ventanas cacheadas: `v3_windows.json` = **5749** (tokenización ≈ 154 s, una sola vez).
   - Entrenamiento por chunks de 400 secuencias con checkpoint tras cada chunk en
     `data/neural/scaled_v3_train/{model.json,state.json}`; reanudable con `--max-min`.
   - Config v3: vocab 2048, embed 128, 8 heads, 4 layers, seq 256 = 1,345,792 params.
   - Pasada 1 completa (5749/5749): loss ≈ 7.69 → 7.687 (prácticamente plana).
   - Pasada 2 (a lr 2e-4) y prueba a lr 1e-3: loss clavada en ~7.69.
3. `data/neural/gen_v3.py` corregido (línea `encode_next` sobrante) y validado:
   la generación sigue siendo basura incoherente — esperado: la loss no ha bajado
   del atractor ~7.69 (floor uniforme ln 2048 ≈ 7.62).

## Diagnóstico y HALLAZGO CLAVE (lr)
- La equivalencia por lotes (B=1 bit-exacto, B>1 batch-mean) sigue VERDE (e-15/e-16).
- El modelo se entrenó pero con lr 2e-4/1e-4 quedaba clavado en un atractor ~7.69
  (solo bajaba ~0.007 en 3 pasadas completas).
- **lr inicial 1e-2 con reset de `done`/`step` por pasada ROMPE el atractor**:
  - pasada 1: 7.64, pasada 2: 7.58, 3: 7.51, 4: 7.44, 5: 7.36, 6: 7.27, 7: 7.19,
    8: 7.04, 9: 6.98, 10: 6.93, 11: 6.88 (loss del chunk 5200, fijo entre pasadas).
  - acc sube de 0.0% → 5.9%.
  - La pérdida reportada es del ÚLTIMO chunk procesado (no media del corpus), y el
    presupuesto de 68 min deja fuera las últimas ~549 ventanas (índice >5200).
- Generación a loss 6.88: sigue sin emitir palabras (a temp baja genera solo espacios;
  a temp alta mezcla símbolos raros). El modelo aprende sobre todo unigramas y el
  patrón "palabra→espacio"; la estructura frásica aún no aparece. NO promocionar.

## Receta para continuar (consola única, fiable)
```bash
R=/home/samuel/A.R.I.A-main
# IMPORTANTE: lr 1e-2 con reset POR PASADA (el schedule colapsa el lr dentro de la
# pasada: 0.95^(step//50)); loss actual ~6.88 tras 11 pasadas a 1e-2.
python - "$R/data/neural/scaled_v3_train/state.json" << 'EOF'
import json,sys
p=sys.argv[1]; s=json.load(open(p)); s.update({'epoch':0,'done':0,'step':0})
open(p,'w').write(json.dumps(s))
EOF
# lanzar en tramos largos hasta done=5200 (una pasada ≈ 70 min):
for i in $(seq 1 55); do
  if [ -d "$R/app" ] && [ -f "$R/.venv/bin/activate" ] && [ -d "$R/data/scaled_model_v3/gpt_model" ]; then
    source "$R/.venv/bin/activate" && python "$R/data/neural/retrain_v3.py" --max-min 68 --chunk 400 --batch 32 --lr 1e-2 >> "$R/data/neural/retrain_v3.log" 2>&1
    break
  fi; sleep 5
done
```
Repetir hasta que la generación monte palabras: probar con `gen_v3.py` cada pocas
pasadas (a temp 0.3 hoy solo emite espacios). Meta realista a largo plazo: loss < 6.3.

## ADVERTENCIA IMPORTANTE (filesystem)
Existen dos rutas físicas: `/home/samuel/A.R.I.A-main` (PUNTO, el repo real con git
y .venv) y `/home/samuel/A.R.I-A-main` (GUION, sin git/.venv). Las vistas son
intermitentes (unas invocaciones ven una, otras la otra). Regla de oro:
- El repo canónico es el PUNTO; los `Write`/`Edit` suelen caer en el GUION.
- Tras escribir un script con `Write`: `cp <GUION-archivo> <PUNTO-archivo>`.
- Los runs de entrenamiento solo ven el PUNTO; leer estado con `cat` del PUNTO.

## Siguiente
- Darle ~5-10 pasadas más (reset + tramos) y revisar si la loss baja de ~7.3.
- Si sigue sin bajar, probar lr 1e-3 estable UNA pasada y observar overfit/divergencia.
- Verificar con `gen_v3.py` cada pocas pasadas.