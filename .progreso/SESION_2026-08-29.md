# PROGRESO A.R.I.A — 2026-08-29 (Sesión 1)

## Objetivo de la sesión
Subir ARIA al ESP32, dejarla al 100% en el PC, y comenzar la **Fase 2**
(visión ambiciosa: que ARIA sea su propio LLM, aprenda del internet y no
dependa de otro proveedor).

## Qué se hizo (funciona)

### 1. ESP32 — firmware subido y verificado
- Proyecto: `firmware/lacal-IA-node` (PlatformIO, placa `esp32dev`).
- `pio run` compiló OK (Flash 86.3%, RAM 36.0%).
- `pio run -t upload` subió 1,137,312 bytes a `/dev/ttyUSB0`, verificado OK.
- Serial boot OK: se conectó a Wi-Fi (IP 192.168.1.20), IA embebida lista,
  esperando frase. El firmware ejecuta TensorFlow Lite Micro local (detector
  de "comando" en frases) SIN depender del servidor para la inferencia.
- Monitor interactivo requiere terminal propia: `pio device monitor -b 115200`.

### 2. PC — ARIA verificada al 100%
- `pytest`: **282/282 tests pasan** (7.7 s). Incluye cerebro neural, memoria,
  tools, voz, web, satélite, detector ESP32.
- Cerebro neural GPT propio: **377,472 params** en `data/aria_gpt/`, 100%
  local, sin API keys. Probado con "¿Qué hora es?" → respondió correctamente.
- Sola advertencia: `ruff` reporta ~39 avisos de ESTILO (orden de imports), no
  de funcionalidad.

### 3. Fase 2 — pipeline de "aprender del internet" creado (nuevo)
- **`scripts/learn_from_web.py`** (NUEVO): baja artículos reales de Wikipedia
  en español (API REST, sin key), los convierte en pares de pregunta/respuesta
  y textos de conocimiento, y los acumula en `data/neural/corpus.json`.
  - Probado: `--articulos 2` → +8 conversaciones, +2 textos.
- **`scripts/retrain_full.py`** (MODIFICADO): ahora también carga y fusiona el
  corpus aprendido de `data/neural/corpus.json` con los datos base, así cada
  reentrenado incluye lo aprendido del internet.
- Corpus actual: `data/neural/corpus.json` = **8 conversaciones + 2 textos**.

## Métricas medidas (importantes)

### El entrenador es EL cuello de botella (hallazgo crítico)
| Medición | Valor |
|---|---|
| 1 época con 118 secuencias (corpus actual) | **168 s (~2.8 min)** |
| Tokenizer BPE (1971 merges) | ~95 s |
| Reentrenado completo de hoy (12 épocas) | **~40-45 min** |
| Arquitectura del entrenador | Python puro, lazos explícitos |

Conclusión: **el entrenador Python-puro es inviable para escalar** ("aprender
del internet" = miles de secuencias). Resolver esto es la prioridad #1 de la
Fase 2 ANTES de crecer datos/modelo.

## Hallazgo estratégico / decisión pendiente
El usuario quiso entender a fondo las opciones. Se le explicaron 3 caminos:
1. **NumPy vectorizado** (recomendado): 10-50x más rápido, sin dependencias
   nuevas, preserva "ARIA = su propio LLM Python puro".
2. **Migrar a framework** (PyTorch/micrograd): más rápido, pero rompe
   "Python puro" y agrega dependencia.
3. **No tocar el entrenador**: frena la meta del LLM propio.

El usuario pidió que le expliquemos más ANTES de decidir. NO eligió aún.
La siguiente sesión debe: mostrarle la Etapa 1 (vectorizar forward + validar
equivalencia a velocidad) para que decida con datos reales.

## Archivos tocados hoy (por mí)
- NUEVO: `scripts/learn_from_web.py`
- MODIFICADO: `scripts/retrain_full.py` (import json + carga corpus.json)
- NUEVO (datos, gitignored): `data/neural/corpus.json`

Nota: `brain.py`, `train_neural.py`, `talk_to_aria.py`, etc. ya venían con
modificaciones SIN commitear (trabajo en progreso previo del usuario), no los
toqué.

---

## PRIMERAS TAREAS PARA MAÑANA (reactivación)
1. **Validar la Etapa 1 de NumPy**: vectorizar el *forward* del transformer
   (`gpt_model.py`) y comparar que da exactamente las mismas salidas que el
   actual (usar los tests `tests/test_neural.py` como red de seguridad).
2. Mostrar al usuario el aumento de velocidad con datos reales (1 época antes
   vs después) para que confirme la Opción 1 (NumPy) o elija otra.
3. Si confirma: vectorizar el *backward* (Etapa 2) y validar con gradientes
   numéricos (finite differences) + reentrenar el mismo modelo y comparar loss.
4. Recordar siempre: NO tocar los archivos que el usuario tenía modificados de
   antes sin preguntar primero.
